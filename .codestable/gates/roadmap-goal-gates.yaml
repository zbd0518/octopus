version: 1
scope: roadmap-goal
stages:
  feature_design:
    before_approve:
      - id: dod-contract-gate
        mode: blocking
  implementation:
    before_review:
      - id: scope-gate
        mode: blocking
      - id: dod-runner
        mode: blocking
      - id: evidence-pack
        mode: blocking
  review:
    before_pass:
      - id: review-evidence-gate
        mode: blocking
        status: protocol-only
  qa:
    before_acceptance:
      - id: qa-evidence-gate
        mode: blocking
        status: protocol-only
  acceptance:
    before_done:
      - id: acceptance-dod-gate
        mode: blocking
        status: protocol-only
  roadmap_audit:
    before_complete:
      - id: goal-consistency-gate
        mode: blocking
      - id: goal-audit-gate
        mode: blocking
        status: protocol-only
